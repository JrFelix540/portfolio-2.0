import React, { Fragment } from "react";
import Header from "../../components/Header";

const B: React.FC = () => {
    return (
        <Fragment>
            <Header />
        </Fragment>
    );
};

export default B;
